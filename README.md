# shade-art-backend
## Настройка переменных окружения 
-установим пакет dotenv-flow (добавляет поддержку нескольких файлов .env с правильной иерархией) основной пакет dotenv устанавливать отдельно не нужно
```
npm install dotenv-flow
```

.env - значения по умолчанию включаем в git
---
.env.local - реальные значения переменных  исключаем из git
---
